#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include "Soccer.h"
#include <ctime>
using namespace std;

int main() {
    Soccer database = Soccer("soccerTeams.csv");


    // vector<int> cnts = {0, 0, 0, 0, 0, 0};
    int iteration = 100;
    // for (int i = 0; i < iteration; i++) {
    //     cnts[database.testMatch(90, 88, 2)]++;
    // }
    // for (auto it : cnts) {
    //     std::cout << setw(10) << it << setw(10) << (it*1.0)/iteration << endl;
    // }
    // database.matchupTest("usa", "china");
    

}